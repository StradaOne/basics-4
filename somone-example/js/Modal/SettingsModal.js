import UiModal from './UiModal';
// Identification
// Authentification
// Settings

const DIALOG_CLASS_NAME = 'settings-dialog';

export default class SettingsModal extends UiModal {
  constructor() {
    super(DIALOG_CLASS_NAME);
  }
}
